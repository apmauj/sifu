###############################
# Aggregated requirements
# Uso recomendado:
#   pip install -r requirements-core.txt        (mínimo API)
#   pip install -r requirements-excel.txt       (para bootstrap histórico)
#   pip install -r requirements-dev.txt         (herramientas dev/tests)
# Este archivo se mantiene para compatibilidad: incluye core + excel.
###############################
# Updated: 2025-08-18 (security audit)

-r requirements-core.txt
-r requirements-excel.txt

# Dependencia adicional para uploads/multipart
python-multipart==0.0.26  # CVE-2026-40347 fixed in 0.0.26